# Normalize text files to LF in the repository and working tree by default.
* text=auto eol=lf

# Keep native Windows script line endings when those files are added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
